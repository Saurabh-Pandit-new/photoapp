// const express = require('express');
// const router = express.Router();
// const { getPortfolioByPhotographerId, addPortfolioItem, updatePortfolioItem, deletePortfolioItem } = require('../controllers/portfolioController');
// const { authorizePhotographer } = require('../middlewares/poauthMiddleware');

// // Get all portfolio items for a photographer
// router.get('/:photographerId', getPortfolioByPhotographerId);

// // Add a new portfolio item
// router.post('/', authorizePhotographer, addPortfolioItem);

// // Update a portfolio item
// router.put('/:portfolioId', authorizePhotographer, updatePortfolioItem);

// // Delete a portfolio item
// router.delete('/:portfolioId', authorizePhotographer, deletePortfolioItem);

// module.exports = router;
